# Copy this file to .env for local development.
# Never commit .env or real credentials.
VITE_API_URL=http://localhost:5000/api
# Set this to the exact frontend tunnel hostname when using Cloudflare Tunnel.
# Example: VITE_ALLOWED_HOST=example.trycloudflare.com
VITE_ALLOWED_HOST=
